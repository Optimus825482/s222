# Root-level Python requirements are for non-backend/local utility workflows only.
# Backend runtime dependencies are owned exclusively by backend/requirements.txt.
# Pinning strategy: use lower-bound pins (>=) for developer flexibility.

streamlit>=1.40.0
openai-whisper>=20231117
